This is a VBA script that allows you to send as many emails as you want from a spreadsheet. The only addition needed is the curated body paragraph. This can be customixed very easily to fit certain columns, as the logic flows pretty well.

At the end - .Display will display each email before sending, changing it to .Send will just automatically send them
